<% provide(:title, "Edit Comment") %>
<div class="info new-comment">
	<p></p>
	<h1>Edit Comment</h1>
	<br>
	<div class="container-fluid">
    <div class="col-md-6 col-md-offset-3">
			<%= form_for @comment, url: edit_comment_path do |f| %>
				<%= render 'shared/error_messages_comments' %>

			  <%= f.label :comment %>
			  <%= f.text_area :body, class: 'form-control' %>

			  <%= f.submit "Update", class: "btn btn-success" %>
			  <%= link_to "Back", "/forum/#{@subject.name}/#{@comment.post.id}", class: "btn btn-primary" %>
			<% end %>
		</div>
	</div>
</div>