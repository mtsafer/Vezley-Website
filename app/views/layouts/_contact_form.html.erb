<%= form_for(@user, url: contact_path) do |f| %>
	<%= render 'shared/error_messages' %>

  <%= f.label :email %>
  <%= f.text_area :body, class: 'form-control' %>

  <%= f.submit "Send Message", class: "btn btn-primary" %>
<% end %>